{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Comment creation</h1>

    <form action="{{ path('JokerJokeBundle_comment_create', { 'post_id' : comment.post.id }) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

{% endblock %}